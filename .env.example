# Server only — never prefix with NEXT_PUBLIC_.
# Same project the bot writes to. Secret key stays on Vercel, not in the browser.
SUPABASE_URL=https://duuozrmornbeburedfsd.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...
